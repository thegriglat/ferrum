[package]
name = "terminator-rate-limited"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib"]

[dependencies]
async-trait = "0.1"
bytes = "1"
ferrum-sdk = { path = "../../../ferrum-sdk" }
